import type { VariantInput } from './types';

export function validateVariantInput(_input: VariantInput): string[] {
  const errors: string[] = [];
  // TODO: implement validation rules
  return errors;
}
