"use client";

import type { LCCResult } from "@/engine/types";

interface CostEvolutionLineProps {
  result: LCCResult;
}

export function CostEvolutionLine({ result: _result }: CostEvolutionLineProps) {
  // Placeholder -- full implementation in Task 2
  return <div className="h-[300px]" />;
}
