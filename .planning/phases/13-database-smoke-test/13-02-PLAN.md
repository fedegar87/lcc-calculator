---
phase: 13-database-smoke-test
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: []
autonomous: false
requirements: [E2E-01, E2E-05]

must_haves:
  truths:
    - "npm run dev starts the application and loads the login page"
    - "npm test runs all 152+ engine tests with 0 failures"
  artifacts: []
  key_links:
    - from: "next.config.ts"
      to: "http://localhost:3000"
      via: "npm run dev"
      pattern: "Ready"
    - from: "vitest.config.ts"
      to: "src/**/*.test.ts"
      via: "npm test"
      pattern: "Tests.*passed"
---

<objective>
Verify the application starts and all engine tests pass -- the smoke test for a working local dev environment.

Purpose: Confirm the full stack is operational after database setup. No new code expected; diagnose and fix any startup or test failures.
Output: Passing dev server, passing test suite, confidence the local dev environment works end-to-end.
</objective>

<execution_context>
@C:/Users/FGarzia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FGarzia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-database-smoke-test/13-01-SUMMARY.md
@package.json
@.env
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify engine tests pass</name>
  <files></files>
  <action>
  Run the full engine test suite:
  ```bash
  npx vitest run
  ```

  Expected: All 152+ tests pass with 0 failures.

  If tests fail:
  - Read the failure output carefully
  - Most likely causes: import path issues, missing dependencies, TypeScript compilation errors
  - Fix any issues found -- these should be minor since the engine tests passed during v1.0
  - Re-run tests until all pass
  - If the failures are in non-engine tests (e.g., component tests that need DOM), those are not blockers for E2E-05 which specifically covers "engine tests"
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All 152+ engine tests pass with 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Verify dev server starts</name>
  <files></files>
  <action>
  Start the Next.js dev server and verify it responds:

  1. Start the dev server in the background:
     ```bash
     npm run dev &
     DEV_PID=$!
     ```

  2. Wait for it to be ready (up to 30 seconds):
     ```bash
     for i in $(seq 1 30); do
       curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 2>/dev/null && break
       sleep 1
     done
     ```

  3. Verify the server responds with HTTP 200 or 302 (redirect to login):
     ```bash
     HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000)
     ```
     Accept 200, 302, or 307 as success (the app may redirect unauthenticated users to /sign-in).

  4. Kill the dev server:
     ```bash
     kill $DEV_PID 2>/dev/null
     ```

  If the server fails to start:
  - Check the error output for missing environment variables, import errors, or build failures
  - Common issues: missing BETTER_AUTH_SECRET, database connection errors, module resolution
  - Fix any issues and retry
  - The .env from Phase 12 should have all required variables
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000</automated>
  </verify>
  <done>Dev server starts, responds to HTTP requests at localhost:3000 (200/302/307)</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <what-built>Complete local dev environment: database with tables and seed data, working dev server, passing engine tests</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in a browser -- you should see the login page
    2. Confirm "LCCzero" branding or login form is visible
    3. The dev server was started by Task 2 -- if it was stopped, run `npm run dev` to restart
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` shows all tests passing
- `npm run dev` starts without errors
- localhost:3000 responds with a page (login/redirect)
</verification>

<success_criteria>
- All 152+ engine tests pass with 0 failures
- Dev server starts and responds at localhost:3000
- No runtime errors in server console output
</success_criteria>

<output>
After completion, create `.planning/phases/13-database-smoke-test/13-02-SUMMARY.md`
</output>
