---
phase: 02-excel-workbook-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit/extract_en15459.py
  - scripts/audit/extract_energy_sources.py
  - scripts/audit/extract_formulas.py
  - scripts/output/en15459.json
  - scripts/output/energy_sources.json
  - scripts/output/formulas_raw.json
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "EN 15459 JSON contains exactly 79 HVAC components with lifespan and maintenance % fields"
    - "Energy sources JSON contains exactly 19 entries with index, name, and category fields"
    - "Formulas raw JSON contains formula cells from all 7 sheets with cell reference, formula text, and cached value"
    - "All extraction scripts run successfully with zero assertion failures"
  artifacts:
    - path: "scripts/audit/extract_en15459.py"
      provides: "EN 15459 component extraction from Calc!B405:H483"
    - path: "scripts/audit/extract_energy_sources.py"
      provides: "Energy source extraction from PI rows 131-149"
    - path: "scripts/audit/extract_formulas.py"
      provides: "Programmatic formula extraction from all 7 sheets"
    - path: "scripts/output/en15459.json"
      provides: "79 HVAC components with lifespan and maintenance data"
    - path: "scripts/output/energy_sources.json"
      provides: "19 energy sources with index and category"
    - path: "scripts/output/formulas_raw.json"
      provides: "Raw formula dump from all 7 sheets"
  key_links:
    - from: "scripts/audit/extract_en15459.py"
      to: "CRAVEzero/200512_LCC_tool_beta_v2.xlsm"
      via: "openpyxl data_only=True"
      pattern: "Calc.*B405.*H483"
    - from: "scripts/audit/extract_energy_sources.py"
      to: "CRAVEzero/200512_LCC_tool_beta_v2.xlsm"
      via: "openpyxl data_only=True"
      pattern: "Project Information.*131.*149"
    - from: "scripts/audit/extract_formulas.py"
      to: "CRAVEzero/200512_LCC_tool_beta_v2.xlsm"
      via: "openpyxl dual-pass (data_only=False + data_only=True)"
      pattern: "startswith.*="
---

<objective>
Programmatically extract all data tables and formulas from the CRAVEzero Excel workbook into machine-readable JSON artifacts.

Purpose: Create the structured data foundation that the formula map (Plan 02), Prisma schema (Phase 3), and calculation engine (Phase 4) will consume. After this plan, no one needs to open the Excel file for data extraction again.
Output: 3 Python scripts in `scripts/audit/`, 3 JSON files in `scripts/output/`
</objective>

<execution_context>
@C:/Users/FGarzia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FGarzia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-excel-workbook-audit/02-RESEARCH.md
@CRAVEzero/200512_LCC_tool_beta_v2.xlsm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EN 15459 and energy sources extraction scripts with validated JSON output</name>
  <files>scripts/audit/extract_en15459.py, scripts/audit/extract_energy_sources.py, scripts/output/en15459.json, scripts/output/energy_sources.json</files>
  <action>
Create two Python extraction scripts using openpyxl. Both scripts must create `scripts/output/` directory if it doesn't exist.

**Script 1: `scripts/audit/extract_en15459.py`**
- Open workbook with `openpyxl.load_workbook("CRAVEzero/200512_LCC_tool_beta_v2.xlsm", data_only=True, keep_vba=True)`
- Access `Calc` sheet
- Extract rows 405-483 (79 components) from columns B-H:
  - B: Component name (string)
  - C: Lifespan min (integer)
  - D: Lifespan max (integer)
  - E: Lifespan average (integer, Excel formula `=ROUND(AVERAGE(C,D),0)`)
  - F: Maintenance % min (decimal, 0-0.15 range)
  - G: Maintenance % max (decimal, 0-0.15 range)
  - H: Maintenance % average (decimal, Excel formula `=AVERAGE(F,G)`)
- Skip rows where column B is None (filter sentinel rows)
- Use 1-based index (`row - 404`) matching Excel INDEX() usage
- Wrap in metadata envelope: `{"source": "EN 15459:2018 via CRAVEzero workbook", "extracted_from": "Calc!B405:H483", "extraction_date": "...", "count": N, "components": [...]}`
- Assertions after extraction:
  - Exactly 79 components
  - First component name is "Air conditioning units"
  - Last component name is "Wiring"
  - All lifespans > 0
  - Maintenance % in range 0-0.15 where not None (Chimney row 414 has None)
- Write to `scripts/output/en15459.json` with `indent=2, ensure_ascii=False`

**Script 2: `scripts/audit/extract_energy_sources.py`**
- Open workbook with `data_only=True, keep_vba=True`
- Access `Project Information` sheet
- Extract rows 131-149 (19 entries) from column C (name):
  - Rows 131-141: category "fuel_source" (row 131 is header "Fuel Source", still include it)
  - Rows 142-149: category "energy_carrier"
- Include `is_header: true` for row 131 (it's the "Fuel Source" category header, INDEX(1) maps here)
- Use 1-based index (`row - 130`)
- Wrap in metadata envelope: `{"source": "CRAVEzero workbook Project Information sheet", "extracted_from": "PI!C131:C149", "extraction_date": "...", "count": N, "note": "Index 1 maps to row 131 (header row). Valid selectable sources are indices 2-19 (rows 132-149).", "sources": [...]}`
- Assertions:
  - Exactly 19 entries (including header row)
  - Index 2 name is "Oil"
  - Index 19 name is "Energy produced by local wind plants"
- Write to `scripts/output/energy_sources.json` with `indent=2, ensure_ascii=False`

Both scripts must use `pathlib.Path` for cross-platform paths and include `if __name__ == "__main__":` guard. Print a summary line on success (e.g., "Extracted 79 EN 15459 components to scripts/output/en15459.json").
  </action>
  <verify>
    <automated>cd C:/llc-calculator-app && python scripts/audit/extract_en15459.py && python scripts/audit/extract_energy_sources.py && python -c "import json; d=json.load(open('scripts/output/en15459.json')); assert d['count']==79; print('EN15459 OK')" && python -c "import json; d=json.load(open('scripts/output/energy_sources.json')); assert d['count']==19; print('Energy OK')"</automated>
  </verify>
  <done>en15459.json has 79 components with all fields populated (except Chimney maintenance=None). energy_sources.json has 19 entries with correct categories. Both scripts exit 0 with all assertions passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create formula extraction script with dual-pass output</name>
  <files>scripts/audit/extract_formulas.py, scripts/output/formulas_raw.json</files>
  <action>
Create a Python script that extracts all formula cells from all 7 sheets using dual-pass technique.

**Script: `scripts/audit/extract_formulas.py`**
- Pass 1: Open workbook with `data_only=False, keep_vba=True` to get formula text
- Pass 2: Open workbook with `data_only=True, keep_vba=True` to get cached computed values
- Iterate all 7 sheets in order: "Project Information", "WLC", "Construction cost", "Maintenance", "Results", "Calc", "Charts"
- For each cell in each sheet, if the cell value starts with "=", record:
  - `sheet`: sheet name
  - `cell`: cell coordinate (e.g., "D8")
  - `row`: row number
  - `col`: column number
  - `formula`: formula text from Pass 1
  - `cached_value`: cached value from Pass 2 (can be None, number, string, or bool)
- Group formulas by sheet in the output
- Wrap in metadata envelope:
  ```json
  {
    "source": "CRAVEzero workbook formula extraction",
    "workbook": "CRAVEzero/200512_LCC_tool_beta_v2.xlsm",
    "extraction_date": "...",
    "sheets": {
      "Project Information": {"count": N, "formulas": [...]},
      "WLC": {"count": N, "formulas": [...]},
      ...
    },
    "total_formulas": N
  }
  ```
- Assertions after extraction:
  - All 7 sheets present in output
  - Total formula count > 500 (workbook has thousands of formulas)
  - Calc sheet has > 200 formulas
  - Results sheet has > 100 formulas
- Print per-sheet formula count summary
- Write to `scripts/output/formulas_raw.json` with `indent=2, ensure_ascii=False`

Handle serialization edge cases: cached_value may be `datetime.datetime` (convert to ISO string), `None` (leave as null), or numeric (leave as-is). Use a custom JSON encoder if needed.

NOTE: The "Construction cost" and "Maintenance" sheets are large (1351 and 908 rows respectively). The script may take 30-60 seconds to run. This is expected.
  </action>
  <verify>
    <automated>cd C:/llc-calculator-app && python scripts/audit/extract_formulas.py && python -c "import json; d=json.load(open('scripts/output/formulas_raw.json')); assert d['total_formulas']>500; assert len(d['sheets'])==7; print(f'Total formulas: {d[chr(39)+chr(39).join([chr(116),chr(111),chr(116),chr(97),chr(108),chr(95),chr(102),chr(111),chr(114),chr(109),chr(117),chr(108),chr(97),chr(115)])]}')" 2>/dev/null || python -c "import json; d=json.load(open('scripts/output/formulas_raw.json')); assert d['total_formulas']>500; assert len(d['sheets'])==7; print(f'Total: {d[\"total_formulas\"]} formulas across 7 sheets')"</automated>
  </verify>
  <done>formulas_raw.json contains formula cells from all 7 sheets with formula text and cached values. Total formula count exceeds 500. Script exits 0 with all assertions passing.</done>
</task>

</tasks>

<verification>
1. All 3 scripts run without errors: `python scripts/audit/extract_en15459.py && python scripts/audit/extract_energy_sources.py && python scripts/audit/extract_formulas.py`
2. en15459.json has exactly 79 components with valid lifespan and maintenance data
3. energy_sources.json has exactly 19 entries covering fuel sources and energy carriers
4. formulas_raw.json has formulas from all 7 sheets with >500 total formulas
5. All JSON files are valid and parseable
</verification>

<success_criteria>
- Three Python scripts in `scripts/audit/` run successfully against the CRAVEzero workbook
- Three JSON files in `scripts/output/` contain validated, structured data
- EN 15459 table matches expected 79-component count with correct first/last entries
- Energy sources match expected 19-entry count with correct index mapping
- Formula dump covers all 7 sheets with both formula text and cached values
</success_criteria>

<output>
After completion, create `.planning/phases/02-excel-workbook-audit/02-01-SUMMARY.md`
</output>
