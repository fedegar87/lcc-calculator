---
phase: 15-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "README contains prerequisites section listing Node.js, Docker, and npm versions"
    - "README contains step-by-step instructions: clone, configure .env, start Docker, migrate, seed, run dev server"
    - "A developer following only the README can reach a working login page without asking questions"
  artifacts:
    - path: "README.md"
      provides: "Complete setup-to-run instructions"
      min_lines: 80
  key_links: []
---

<objective>
Replace the default Next.js boilerplate README with a comprehensive quickstart guide that takes a new developer from `git clone` to a working login page.

Purpose: Fulfill INFRA-03 — step-by-step setup instructions so anyone cloning the repo can run LCCzero locally without asking questions.
Output: README.md at project root
</objective>

<execution_context>
@C:/Users/FGarzia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FGarzia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@docker-compose.yml
@package.json
@.env
@prisma/seed-data/shared.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Replace the entire contents of README.md with a quickstart-focused guide. Structure:

1. **Project intro** (1-2 sentences): "LCCzero is a Life Cycle Cost calculator for buildings, replacing the CRAVEzero Excel workbook with a modern web application."

2. **Prerequisites** section listing exact version requirements:
   - Node.js >= 20
   - npm >= 10
   - Docker and Docker Compose (for PostgreSQL)

3. **Quickstart** section with numbered steps:
   1. Clone the repository (use the GitLab URL: `https://gitlab.inf.unibz.it/Federico.Garzia/lcc-calculator.git`)
   2. Install dependencies: `npm install`
   3. Start PostgreSQL: `docker compose up -d`
   4. Generate Prisma client: `npx prisma generate`
   5. Run database migrations: `npx prisma migrate deploy`
   6. Seed demo data: `npx prisma db seed`
   7. Start dev server: `npm run dev`
   8. Open `http://localhost:3001` and log in with demo credentials

   Show the 4 environment variables inline during step 3 (explain they're already configured in `.env` with defaults matching docker-compose.yml — no manual setup needed):
   - DATABASE_URL=postgresql://lccuser:lccpass@localhost:5432/lccdb?schema=public
   - BETTER_AUTH_SECRET=lcczero-dev-secret-do-not-use-in-production
   - BETTER_AUTH_URL=http://localhost:3001
   - EXPORT_DIR=./exports

4. **Demo credentials** section: email `demo@lcczero.dev`, password `demo123`

5. **Scripts** section — a markdown table of npm scripts:
   | Script | Description |
   | `npm run dev` | Start dev server (Turbopack) |
   | `npm run build` | Production build |
   | `npm run test` | Run tests (Vitest) |
   | `npm run test:run` | Run tests once (CI) |
   | `npm run lint` | ESLint |
   | `npm run db:generate` | Generate Prisma client |
   | `npm run db:migrate` | Create migration |
   | `npm run db:seed` | Seed demo data |
   | `npm run db:studio` | Open Prisma Studio |

6. **Troubleshooting** section with 4 items:
   - **Docker not running**: `docker compose up -d` fails → install/start Docker Desktop
   - **Port 5432 in use**: another PostgreSQL instance running → stop it or change port in docker-compose.yml and DATABASE_URL
   - **Prisma migration fails**: ensure PostgreSQL is healthy (`docker compose ps`), check DATABASE_URL matches docker-compose.yml credentials
   - **Auth redirect issues**: verify BETTER_AUTH_URL matches dev server port (3001)

7. **Known limitations** section (brief):
   - PDF/Excel export is disabled due to Recharts/RSC incompatibility (charts cannot render in React Server Components)

Written in English, neutral technical tone. No architecture overview, no contributing guidelines, no tech stack section. Use code blocks for all commands. Use `bash` language tag for shell commands.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); const checks=['Prerequisites','Quickstart','docker compose','prisma','npm run dev','localhost:3001','demo@lcczero.dev','Troubleshooting','Known']; const missing=checks.filter(c=>!r.includes(c)); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All sections present')}"</automated>
  </verify>
  <done>README.md contains all required sections: project intro, prerequisites with versions, numbered quickstart steps, demo credentials, scripts table, troubleshooting, known limitations. A developer reading only this file can reach a working login page.</done>
</task>

</tasks>

<verification>
- README.md exists and is not the default Next.js boilerplate
- Contains "Prerequisites" section with Node.js, Docker, npm version requirements
- Contains numbered "Quickstart" steps from clone through login
- Contains demo credentials (demo@lcczero.dev / demo123)
- Contains "Troubleshooting" section with at least 4 items
- Contains "Known Limitations" noting Recharts/RSC issue
- Contains npm scripts reference table
</verification>

<success_criteria>
README.md replaces boilerplate with complete quickstart guide satisfying all 3 Phase 15 success criteria from the roadmap.
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation/15-01-SUMMARY.md`
</output>
