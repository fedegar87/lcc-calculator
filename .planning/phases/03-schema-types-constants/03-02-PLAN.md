---
phase: 03-schema-types-constants
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - src/engine/constants.ts
  - src/engine/validation.ts
autonomous: true
requirements: [DATA-07, DATA-08, DATA-09]

must_haves:
  truths:
    - "Engine types are importable without any Prisma or framework dependency"
    - "EN 15459 constants contain 79 HVAC components with lifespan and maintenance data"
    - "Energy sources contain 18 selectable sources (index 2-19, excluding header)"
    - "validateVariantInput rejects out-of-range inputs and returns error messages"
    - "FormulaMode is a string literal union type, not a Prisma enum import"
    - "CATEGORY_MAINTENANCE_MAP correctly classifies all 21 cost categories"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "Pure TS interfaces for engine I/O"
      exports: ["ENGINE_VERSION", "FormulaMode", "EngineConfig", "VariantInput", "LCCResult", "YearlyEnergyCosts", "CATEGORY_MAINTENANCE_MAP", "END_USE_PAIRS"]
    - path: "src/engine/constants.ts"
      provides: "EN 15459 components and energy sources from audit JSON"
      exports: ["EN15459_COMPONENTS", "ENERGY_SOURCES", "getEN15459Component", "getEnergySource"]
    - path: "src/engine/validation.ts"
      provides: "Input validation with plausible range checks"
      exports: ["validateVariantInput"]
  key_links:
    - from: "src/engine/constants.ts"
      to: "scripts/output/en15459.json"
      via: "JSON import with resolveJsonModule"
      pattern: "import.*en15459"
    - from: "src/engine/constants.ts"
      to: "scripts/output/energy_sources.json"
      via: "JSON import with resolveJsonModule"
      pattern: "import.*energy_sources"
    - from: "src/engine/validation.ts"
      to: "src/engine/types.ts"
      via: "import VariantInput"
      pattern: "import.*VariantInput"
---

<objective>
Define the pure TypeScript engine types, import EN 15459 constants from audit JSON, and create input validation.

Purpose: Establish the engine's type system that is completely decoupled from Prisma/Next.js. The calculation engine (Phase 4) implements against these types. The tRPC layer (Phase 7) bridges between Prisma types and engine types.
Output: Three files in `src/engine/`: types.ts (interfaces), constants.ts (EN 15459 data), validation.ts (range checks).
</objective>

<execution_context>
@C:/Users/FGarzia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FGarzia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schema-types-constants/03-CONTEXT.md
@.planning/phases/03-schema-types-constants/03-RESEARCH.md
@scripts/output/en15459.json
@scripts/output/energy_sources.json
@llc-implementation-plan.md (lines 1095-1330 -- TASK 3: Engine Types & Constants)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create engine type interfaces and domain constants</name>
  <files>src/engine/types.ts</files>
  <action>
Remove `src/engine/.gitkeep` and create `src/engine/types.ts` with all engine type definitions from the implementation plan TASK 3 section 3.1. This file MUST have zero imports from Prisma, Next.js, or any framework.

Add a header comment: `// PURE ENGINE TYPES - No Prisma or framework imports allowed`

**Exports (in order):**

1. `ENGINE_VERSION = "1.0.0"` constant.

2. `FormulaMode` type: `'excel_replica' | 'excel_bugfixed'` (string literal union, NOT imported from Prisma).

3. `EngineConfig` interface: `{ formulaMode: FormulaMode; maxReplacementCycles: number }`.

4. `DEFAULT_ENGINE_CONFIG` constant: `{ formulaMode: 'excel_bugfixed', maxReplacementCycles: 3 }`.

5. `MaintenanceType` type: `'building_element' | 'building_service' | 'none'`.

6. `CATEGORY_MAINTENANCE_MAP` constant: `Record<string, MaintenanceType>` mapping all 21 CostCategory values.
   - A1-A10: `'building_element'`
   - B1-B6, C1-C3: `'building_service'`
   - D1, E1: `'none'`

7. `END_USE_PAIRS` constant: `[string, string | null][]`
   - `['HEATING_1', 'HEATING_2']`, `['COOLING_1', 'COOLING_2']`, `['DHW_1', 'DHW_2']`, `['HOUSEHOLD_ELECTRICITY', null]`
   - Add comment: PV_PRODUCTION handled separately (subtracted, not added)

8. Input interfaces (all use plain `number`, not Decimal.js):
   - `EnergySourcePrice`: `{ index: number; name: string; pricePerKwh: number; annualIncrease: number }`
   - `EnergyEndUseInput`: `{ endUse: string; energySourceIndex: number; specificConsumption: number; pvProductionKwh?: number }`
   - `ServiceComponentInput`: `{ name: string; constructionCost: number; en15459ComponentIndex: number }`
   - `CostItemInput`: `{ category: string; materialCost: number; laborCost: number; otherCost: number }`
   - `DesignCostInput`: `{ lineNumber: number; description: string; preliminaryCost: number; definitiveCost: number; executiveCost: number; siteManagementCost: number }`
   - `IncomeInputData`: `{ rents: { monthlyPerM2: number; area: number; taxes: number }[]; otherIncomes: { amount: number; taxes: number }[]; expectedPricePerM2?: number }`
   - `WLCInputData`: `{ landCost: number; enablingCosts: number; planningFees: number; userSupportPropMgmt: number; userSupportCharges: number; userSupportAdmin: number; financeCost: number; designCostsTotal: number; siteManagementCostsTotal: number }`

9. `VariantInput` interface -- the main engine input. Fields:
   - `referencePeriod: number`, `interestRate: number`, `inflationRate: number`
   - `treatedFloorArea: number`
   - `energyPrices: EnergySourcePrice[]`, `energyInputs: EnergyEndUseInput[]`
   - `costItems: CostItemInput[]`, `serviceComponents: ServiceComponentInput[]`
   - `buildingElementMaintenancePercent: number`
   - `wlcInput: WLCInputData`, `designCosts: DesignCostInput[]`
   - `incomeInput?: IncomeInputData`

10. `YearlyEnergyCosts` interface: `{ nominal: number[]; actualized: number[]; cumulated: number[] }`

11. `LCCResult` interface -- complete engine output. Include ALL fields from implementation plan:
    - engineVersion, formulaMode
    - realInterestRate
    - heatingCosts, coolingCosts, dhwCosts, householdCosts, pvProduction (all YearlyEnergyCosts)
    - maintenanceElements, maintenanceServices, maintenanceTotal, maintenanceCumulated (all number[])
    - totalMaterials, totalLabor, totalConstruction
    - constructionByCategory: Record<string, number>
    - nonConstructionCosts, designCosts, buildingSiteManagement
    - energyConsumed, energyProduced, maintenanceAtRefPeriod, operationAndMaintenance
    - lcc, wlc
    - residualValue, lccNetResidual
    - income: `{ netAnnualIncome, simplePaybackYears (number | null), npvIncomeStream, netPresentValue } | null`
    - kpiDesignOverLCC, kpiConstructionOverLCC, kpiLaborOverLCC, kpiOMOverLCC (all number | null)
    - kpiLCCPerM2, kpiWLCPerM2 (both number | null)

Add inline comments with formula IDs where applicable (e.g., `// FIN-001`, `// AGG-012`, `// RES-001`).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/engine/types.ts 2>&1</automated>
  </verify>
  <done>
- types.ts compiles without errors
- File has zero imports from Prisma/framework
- All 21 categories are mapped in CATEGORY_MAINTENANCE_MAP
- FormulaMode is a string literal union
- VariantInput and LCCResult interfaces match implementation plan
  </done>
</task>

<task type="auto">
  <name>Task 2: Import EN 15459 constants and energy sources from audit JSON</name>
  <files>src/engine/constants.ts</files>
  <action>
Create `src/engine/constants.ts` that imports the verified data from the Phase 2 audit scripts output.

**CRITICAL:** Import directly from JSON files, do NOT manually transcribe 79 components. `resolveJsonModule: true` is already configured in tsconfig.json.

1. Define `EN15459Component` interface:
   ```typescript
   export interface EN15459Component {
     index: number;
     name: string;
     lifespanMin: number;
     lifespanMax: number;
     lifespanAvg: number;
     maintenancePctMin: number | null;
     maintenancePctMax: number | null;
     maintenancePctAvg: number | null;
   }
   ```

2. Import `scripts/output/en15459.json` and map to `EN15459_COMPONENTS: EN15459Component[]`.
   - Access `en15459Data.components` array (JSON root key is `components`). See RESEARCH.md Pattern 6.
   - Map snake_case JSON keys to camelCase TypeScript properties.
   - The JSON has 79 components with fields: index, name, lifespan_min, lifespan_max, lifespan_avg, maintenance_pct_min, maintenance_pct_max, maintenance_pct_avg.

3. Define `EnergySource` interface:
   ```typescript
   export interface EnergySource {
     index: number;
     name: string;
     category: string;
   }
   ```

4. Import `scripts/output/energy_sources.json` and map to `ENERGY_SOURCES: EnergySource[]`.
   - Access `energySourcesData.sources` array (JSON root key is `sources`). See RESEARCH.md Pattern 6.
   - Filter out the header entry (index 1, `is_header: true`).
   - Result: 18 selectable sources (indices 2-19).

5. Export lookup helpers:
   - `getEN15459Component(index: number): EN15459Component | undefined` -- finds by index
   - `getEnergySource(index: number): EnergySource | undefined` -- finds by index

Use relative path for the JSON imports: `../../scripts/output/en15459.json` and `../../scripts/output/energy_sources.json`.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/engine/constants.ts 2>&1</automated>
  </verify>
  <done>
- constants.ts compiles without errors
- EN15459_COMPONENTS has exactly 79 entries
- ENERGY_SOURCES has exactly 18 entries (header filtered out)
- getEN15459Component(1) returns "Air conditioning units"
- getEnergySource(12) returns "National Electricity-Mix"
- No manual data entry -- all from JSON imports
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create engine input validation with range checks</name>
  <files>src/engine/validation.ts, src/engine/__tests__/validation.test.ts</files>
  <behavior>
    - validateVariantInput with valid input returns empty array
    - referencePeriod <= 0 returns error
    - referencePeriod > 100 returns error
    - interestRate < -0.1 returns error ("out of plausible range")
    - interestRate > 0.5 returns error
    - inflationRate < -0.1 returns error
    - inflationRate > 0.5 returns error
    - treatedFloorArea < 0 returns error
    - treatedFloorArea = 0 is valid (KPIs will be null, but input is valid)
    - energySourceIndex < 1 or > 19 returns error
    - duplicate endUse entries return error
    - EN 15459 component index < 1 or > 79 returns error
    - negative cost in costItems returns error
    - negative buildingElementMaintenancePercent returns error
    - multiple errors accumulate (returns all errors, not just first)
  </behavior>
  <action>
Create `src/engine/validation.ts`:

```typescript
import type { VariantInput } from './types';

export function validateVariantInput(input: VariantInput): string[] {
  const errors: string[] = [];
  // Range checks per implementation plan section 3.3
  // Reference period
  // Interest rate: [-0.1, 0.5]
  // Inflation rate: [-0.1, 0.5]
  // Treated floor area: >= 0
  // Energy source indexes: 1-19
  // Duplicate endUse check
  // EN 15459 component indexes: 1-79
  // Non-negative costs
  // Non-negative maintenance %
  return errors;
}
```

Use plain TypeScript validation (NOT Zod). Returns `string[]` of human-readable error messages. Empty array = valid input.

Then create `src/engine/__tests__/validation.test.ts` with tests for all behaviors listed above. Create a `validInput` fixture that satisfies all rules, then test each rule by mutating one field.

Use `vitest` (already configured). Import from `../validation` and `../types`.
  </action>
  <verify>
    <automated>npx vitest run src/engine/__tests__/validation.test.ts 2>&1</automated>
  </verify>
  <done>
- validation.ts exports validateVariantInput function
- All validation rules from implementation plan are implemented
- All tests pass
- Valid input returns empty array
- Each invalid field produces a descriptive error message
- Multiple errors accumulate correctly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit src/engine/types.ts src/engine/constants.ts src/engine/validation.ts` succeeds
2. `npx vitest run src/engine/__tests__/validation.test.ts` passes all tests
3. No imports from `@/generated/prisma`, `@prisma/client`, or any framework in engine files
4. `grep -r "import.*prisma" src/engine/` returns no results
5. EN15459_COMPONENTS length is 79
6. ENERGY_SOURCES length is 18
</verification>

<success_criteria>
- Engine types define complete VariantInput and LCCResult interfaces matching the implementation plan
- EN 15459 constants are imported from verified audit JSON (no manual transcription)
- Input validation catches all out-of-range values with descriptive messages
- All engine files are pure TypeScript with zero framework dependencies
- All validation tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-types-constants/03-02-SUMMARY.md`
</output>
