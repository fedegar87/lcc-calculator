---
phase: 01-project-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - prisma/schema.prisma
  - src/lib/prisma.ts
  - vitest.config.ts
  - src/server/trpc/init.ts
  - src/server/trpc/router.ts
  - src/server/trpc/client.tsx
  - src/server/trpc/server.tsx
  - src/server/trpc/query-client.ts
  - src/app/api/trpc/[...trpc]/route.ts
  - package.json
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03

must_haves:
  truths:
    - "npm run dev starts the Next.js application without errors"
    - "TypeScript strict mode catches type errors at build time"
    - "npm test executes Vitest with zero configuration issues"
    - "EURAC brand colors and Inter font are configured in the design system"
    - "tRPC endpoint responds at /api/trpc"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tailwind v4 theme with EURAC palette, glass morphism utilities"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font and metadata"
      contains: "Inter"
    - path: "prisma/schema.prisma"
      provides: "Minimal Prisma 7 generator config (no models yet)"
      contains: "prisma-client"
    - path: "src/lib/prisma.ts"
      provides: "PrismaClient singleton with driver adapter"
      contains: "PrismaPg"
    - path: "vitest.config.ts"
      provides: "Vitest config with path aliases and node environment"
      contains: "tsconfigPaths"
    - path: "src/server/trpc/init.ts"
      provides: "tRPC initialization with superjson transformer"
      contains: "initTRPC"
    - path: "src/server/trpc/router.ts"
      provides: "Root tRPC router"
      contains: "createTRPCRouter"
    - path: "src/app/api/trpc/[...trpc]/route.ts"
      provides: "tRPC fetch handler for App Router"
      contains: "fetchRequestHandler"
    - path: "package.json"
      provides: "All npm scripts (dev, build, test, db:*)"
      contains: "vitest"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
    - from: "src/app/api/trpc/[...trpc]/route.ts"
      to: "src/server/trpc/router.ts"
      via: "appRouter import"
      pattern: "import.*appRouter"
    - from: "src/server/trpc/router.ts"
      to: "src/server/trpc/init.ts"
      via: "createTRPCRouter import"
      pattern: "import.*createTRPCRouter"
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "vite-tsconfig-paths plugin reads path aliases"
      pattern: "tsconfigPaths"
---

<objective>
Configure the design system (Tailwind v4 EURAC palette + Inter font), Prisma 7 generator, tRPC 11 boilerplate, Vitest, and all package.json scripts on top of the scaffolded Next.js project.

Purpose: Complete all tooling configuration so the dev environment is fully operational -- `npm run dev` starts the app, `npm test` runs Vitest, and the tRPC/Prisma infrastructure is ready for business logic in later phases.
Output: Fully configured Next.js 15 project with working dev server, test runner, tRPC endpoint, Prisma generator, and EURAC-branded design system.
</objective>

<execution_context>
@C:/Users/FGarzia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/FGarzia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding/01-RESEARCH.md
@.planning/phases/01-project-scaffolding/01-CONTEXT.md
@.planning/phases/01-project-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Tailwind v4 EURAC design system and Inter font</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <action>
**Step 1: Update globals.css with Tailwind v4 CSS-first config**

Replace the contents of `src/app/globals.css` with the EURAC design system. Use `@theme` and `@theme inline` directives (NO `tailwind.config.js` -- Tailwind v4 is CSS-first).

The file should contain:
1. `@import "tailwindcss";` at the top
2. `@import "tw-animate-css";` for shadcn/ui animations
3. `:root` block with shadcn/ui CSS variables in OKLCH color space, mapping EURAC primary (#C8102E) to the `--primary` variable
4. `.dark` block with dark mode overrides
5. `@theme inline` block registering shadcn/ui tokens (--color-background, --color-foreground, --color-primary, --color-primary-foreground, --color-card, --color-muted, --color-border, --color-ring, --radius-*)
6. `@theme` block with EURAC custom palette as static tokens:
   - `--color-eurac-red: oklch(0.48 0.18 27.5)` (#C8102E)
   - `--color-eurac-dark: oklch(0.33 0.01 240)` (#404648)
   - `--color-eurac-gray: oklch(0.47 0.01 240)` (#666B6C)
   - `--color-eurac-light: oklch(0.74 0.01 240)` (#B2B5B5)
   - `--font-sans: var(--font-inter)` to use Inter as default sans font
7. `@layer utilities` with glass morphism classes:
   - `.glass`: white translucent background, backdrop-blur(12px), subtle white border
   - `.glass-dark`: dark translucent background, backdrop-blur(12px), subtle border

Reference the exact CSS from `01-RESEARCH.md` Pattern 1 for the full implementation. Preserve any shadcn/ui CSS variables that were added during `npx shadcn init` -- merge, don't replace.

**Step 2: Update layout.tsx with Inter font**

Update `src/app/layout.tsx` to:
1. Import `Inter` from `next/font/google`
2. Configure with `subsets: ["latin"]`, `weight: ["300", "400", "500", "600", "700"]`, `variable: "--font-inter"`
3. Set metadata: `title: "LCCzero"`, `description: "Life-Cycle Cost Calculator for nZEB"`
4. Apply `${inter.variable} font-sans antialiased` to the `<body>` className

Reference the exact code from `01-RESEARCH.md` "Inter Font Setup in Root Layout" section.

**Step 3: Update page.tsx with minimal placeholder**

Replace the default create-next-app home page with a minimal placeholder:

```tsx
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <h1 className="text-4xl font-bold text-primary">LCCzero</h1>
    </main>
  );
}
```

This confirms the EURAC primary color and Inter font are working visually.

**ANTI-PATTERNS to avoid:**
- Do NOT create a `tailwind.config.js` file. Tailwind v4 is CSS-first.
- Do NOT use hex colors directly in CSS variables. Use OKLCH format for Tailwind v4 compatibility.
- Do NOT remove shadcn/ui CSS variables that were added during init.
  </action>
  <verify>
    <automated>cd C:/llc-calculator-app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - globals.css uses @theme directive with EURAC OKLCH colors (no tailwind.config.js)
    - layout.tsx loads Inter font with weights 300-700 and sets --font-inter CSS variable
    - Page title is "LCCzero"
    - Glass morphism utilities (.glass, .glass-dark) are defined
    - `next build` completes without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up Prisma 7, tRPC 11 boilerplate, Vitest config, and package.json scripts</name>
  <files>prisma/schema.prisma, src/lib/prisma.ts, src/server/trpc/init.ts, src/server/trpc/router.ts, src/server/trpc/client.tsx, src/server/trpc/server.tsx, src/server/trpc/query-client.ts, src/app/api/trpc/[...trpc]/route.ts, vitest.config.ts, package.json</files>
  <action>
**Step 1: Create Prisma 7 minimal schema**

Create `prisma/schema.prisma` (create `prisma/` directory if create-next-app didn't):

```prisma
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added in Phase 3
```

CRITICAL: Use `prisma-client` provider (NOT `prisma-client-js` -- that's Prisma 6 legacy). The `output` field is mandatory in Prisma 7.

**Step 2: Create PrismaClient singleton**

Create `src/lib/prisma.ts`:

```typescript
import { PrismaClient } from "../generated/prisma";
import { PrismaPg } from "@prisma/adapter-pg";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

function createPrismaClient() {
  const adapter = new PrismaPg({
    connectionString: process.env.DATABASE_URL!,
  });
  return new PrismaClient({ adapter });
}

export const prisma = globalForPrisma.prisma ?? createPrismaClient();

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

Note: This file will have import errors until `npx prisma generate` is run with a valid DATABASE_URL. That's expected -- no database is needed in Phase 1.

**Step 3: Create tRPC 11 boilerplate (6 files)**

Create directory `src/server/trpc/` and add these files:

**src/server/trpc/init.ts:**
```typescript
import { initTRPC } from "@trpc/server";
import superjson from "superjson";

export const createTRPCContext = async (opts: { headers: Headers }) => {
  return {
    // Auth context will be added in Phase 7
  };
};

const t = initTRPC
  .context<Awaited<ReturnType<typeof createTRPCContext>>>()
  .create({
    transformer: superjson,
  });

export const createTRPCRouter = t.router;
export const createCallerFactory = t.createCallerFactory;
export const baseProcedure = t.procedure;
```

**src/server/trpc/router.ts:**
```typescript
import { createTRPCRouter, baseProcedure } from "./init";

export const appRouter = createTRPCRouter({
  healthcheck: baseProcedure.query(() => {
    return { status: "ok" };
  }),
});

export type AppRouter = typeof appRouter;
```

**src/server/trpc/query-client.ts:**
```typescript
import { QueryClient, defaultShouldDehydrateQuery } from "@tanstack/react-query";
import superjson from "superjson";

export function makeQueryClient() {
  return new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 30 * 1000,
      },
      dehydrate: {
        serializeData: superjson.serialize,
        shouldDehydrateQuery: (query) =>
          defaultShouldDehydrateQuery(query) || query.state.status === "pending",
      },
      hydrate: {
        deserializeData: superjson.deserialize,
      },
    },
  });
}
```

**src/server/trpc/client.tsx:**
```typescript
"use client";

import type { QueryClient } from "@tanstack/react-query";
import { QueryClientProvider } from "@tanstack/react-query";
import { httpBatchLink } from "@trpc/client";
import { createTRPCContext } from "@trpc/tanstack-react-query";
import { useState } from "react";
import superjson from "superjson";
import { makeQueryClient } from "./query-client";
import type { AppRouter } from "./router";

export const { TRPCProvider, useTRPC } = createTRPCContext<AppRouter>();

let clientQueryClientSingleton: QueryClient;

function getQueryClient() {
  if (typeof window === "undefined") {
    return makeQueryClient();
  }
  return (clientQueryClientSingleton ??= makeQueryClient());
}

function getUrl() {
  const base = (() => {
    if (typeof window !== "undefined") return "";
    if (process.env.VERCEL_URL) return `https://${process.env.VERCEL_URL}`;
    return "http://localhost:3000";
  })();
  return `${base}/api/trpc`;
}

export function TRPCReactProvider(props: { children: React.ReactNode }) {
  const queryClient = getQueryClient();
  const [trpcClient] = useState(() =>
    TRPCProvider.createClient({
      links: [
        httpBatchLink({
          transformer: superjson,
          url: getUrl(),
        }),
      ],
    })
  );

  return (
    <QueryClientProvider client={queryClient}>
      <TRPCProvider client={trpcClient} queryClient={queryClient}>
        {props.children}
      </TRPCProvider>
    </QueryClientProvider>
  );
}
```

Note: The tRPC v11 client pattern may differ slightly from the research doc. If `createTRPCContext` from `@trpc/tanstack-react-query` doesn't export `TRPCProvider` and `useTRPC`, adapt the pattern to match the actual v11 API. The key requirement is: a client-side provider wrapping the app, using `httpBatchLink` with `superjson` transformer.

**src/server/trpc/server.tsx:**
```typescript
import "server-only";
import { createHydrationHelpers } from "@trpc/tanstack-react-query/rsc";
import { cache } from "react";
import { createCallerFactory } from "./init";
import { makeQueryClient } from "./query-client";
import { appRouter } from "./router";
import { createTRPCContext } from "./init";
import { headers } from "next/headers";

const createCaller = createCallerFactory(appRouter);

export const getQueryClient = cache(makeQueryClient);

export const caller = createCaller(async () => {
  const h = await headers();
  return createTRPCContext({ headers: h });
});

export const { trpc, HydrateClient } = createHydrationHelpers<typeof appRouter>(
  caller,
  getQueryClient
);
```

**src/app/api/trpc/[...trpc]/route.ts:**
```typescript
import { fetchRequestHandler } from "@trpc/server/adapters/fetch";
import { createTRPCContext } from "@/server/trpc/init";
import { appRouter } from "@/server/trpc/router";

const handler = (req: Request) =>
  fetchRequestHandler({
    endpoint: "/api/trpc",
    req,
    router: appRouter,
    createContext: () => createTRPCContext({ headers: req.headers }),
  });

export { handler as GET, handler as POST };
```

**Step 4: Create Vitest configuration**

Create `vitest.config.ts` at project root:

```typescript
import { defineConfig } from "vitest/config";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [tsconfigPaths()],
  test: {
    globals: true,
    environment: "node",
    include: ["tests/**/*.test.ts"],
  },
});
```

Use `environment: "node"` (NOT `jsdom`) -- engine tests are pure calculations, no DOM needed.

**Step 5: Create a smoke test**

Create `tests/smoke.test.ts`:

```typescript
import { describe, it, expect } from "vitest";

describe("smoke test", () => {
  it("vitest is configured correctly", () => {
    expect(1 + 1).toBe(2);
  });
});
```

**Step 6: Update package.json scripts**

Add/update these scripts in `package.json`:

```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest",
    "test:run": "vitest run",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio"
  }
}
```

Keep existing scripts from create-next-app (dev, build, start, lint) but update `dev` to use `--turbopack`. Add the test and db scripts.

**Step 7: Wrap layout with TRPCReactProvider**

Update `src/app/layout.tsx` to wrap children with the tRPC provider:

```tsx
import { TRPCReactProvider } from "@/server/trpc/client";

// In the RootLayout body:
<body className={`${inter.variable} font-sans antialiased`}>
  <TRPCReactProvider>
    {children}
  </TRPCReactProvider>
</body>
```

**Step 8: Commit**

```bash
git add -A
git commit -m "feat(01-02): configure design system, Prisma 7, tRPC 11, and Vitest"
```

**ANTI-PATTERNS to avoid:**
- Do NOT install `@trpc/next` -- it's Pages Router only
- Do NOT import PrismaClient from `@prisma/client` -- Prisma 7 uses custom output path
- Do NOT use `environment: "jsdom"` in vitest.config.ts for engine tests
- Do NOT create `tailwind.config.js` -- already handled in Task 1 via CSS-first
- Do NOT use `prisma-client-js` provider -- that's Prisma 6 legacy
  </action>
  <verify>
    <automated>cd C:/llc-calculator-app && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Prisma schema exists with `prisma-client` provider and output path
    - PrismaClient singleton exists at src/lib/prisma.ts
    - tRPC boilerplate: 6 files in src/server/trpc/ + route handler
    - tRPC healthcheck procedure responds (testable once dev server runs)
    - vitest.config.ts uses node environment and vite-tsconfig-paths
    - Smoke test passes with `npm test`
    - package.json has dev, build, test, test:run, db:* scripts
    - layout.tsx wraps children with TRPCReactProvider
    - `npm run dev` starts without errors (manual verify)
  </done>
</task>

</tasks>

<verification>
1. `npm run test:run` passes the smoke test
2. `npm run build` completes without TypeScript or build errors
3. `cat prisma/schema.prisma` shows `prisma-client` provider with output path
4. `ls src/server/trpc/` shows init.ts, router.ts, client.tsx, server.tsx, query-client.ts
5. `ls src/app/api/trpc/*/` shows route.ts
6. `cat vitest.config.ts` shows globals: true, environment: "node"
7. `cat src/app/globals.css | grep @theme` confirms CSS-first Tailwind config
8. `cat src/app/layout.tsx | grep Inter` confirms font setup
9. `npm run dev` starts the application (start and verify manually, then Ctrl+C)
</verification>

<success_criteria>
- `npm run dev` starts Next.js 15 without errors
- `npm run build` completes successfully with TypeScript strict mode
- `npm run test:run` executes Vitest and passes the smoke test
- EURAC primary color (#C8102E via OKLCH) is visible on the placeholder page
- Inter font loads with weights 300-700
- tRPC, Prisma, and Vitest infrastructure is ready for business logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding/01-02-SUMMARY.md`
</output>
